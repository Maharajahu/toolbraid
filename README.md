# ToolBraid

ToolBraid is a secure MCP control plane that routes web actions through the
safest available capability: structured API, WebMCP, DOM/accessibility, then
vision as a last resort.

The rebuild is in progress on `codex/toolbraid-v1`. See
[`docs/WORKPLAN.md`](docs/WORKPLAN.md) for the frozen contracts.
